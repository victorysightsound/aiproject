use anyhow::{bail, Result};
use crate::cli::TaskCommands;

pub fn run(_cmd: TaskCommands) -> Result<()> {
    bail!("task command not yet implemented (Phase 4)")
}

pub fn list() -> Result<()> {
    bail!("tasks command not yet implemented (Phase 4)")
}
